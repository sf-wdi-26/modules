<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>Candy App</title>

<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
<link rel="stylesheet" type="text/css" href="stylesheets/main.css" /></head>
<body>
	<h1>
		<a target="_blank" href="https://www.youtube.com/watch?v=mKli0y-Xr-Q">Candy Shoppe</a>

	</h1>
	<div class="container">
		<% if(user){ %>
			Hello <%= user.fb.firstName %>
			<a class="btn" href="/logout">Logout</a>
		<% } else {  %>
			<a class="btn" href="/login/facebook">Login with Facebook</a>
		<% } %>
		
		<ul>
			<% for(var i=0; i< candies.length; i++) {  %>
		      <li>
		      	<h4><b>Name : </b> <%= candies[i].name %><br></h4>
		      	<b>Color : </b> <%= candies[i].color %>
		      </li>
			<% } %>
		</ul>
	</div>

	<hr>
	<div class="container">
		<% if(user){ %>
			<% include ./partials/candy/form %>
		<% } else {  %>
			You Need to be Logged in to add candies to the list. <br>
			<a class="btn" href="/login/facebook">Login with Facebook</a>
		<% } %>
	</div>

</body>
</html>